<%- include('partials/headerloggedin') -%>
  <div class="container mx-auto bg-blue-50 min-h-screen">
      <ul class=" flex flex-col items-center list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="border m-4 text-center w-56 h-48 flex justify-center bg-white rounded-sm">
            <a href="/post/<%= posts[i]._id%>">
              <div>
                <h2>Strength</h2>
              </div>
              <div class="mt-14">
                <span><%= posts[i].title%></span>
              </div>
            </a>
          </li>
        <% } %>
        </ul>
  </div>  
<%- include('partials/footer') -%>